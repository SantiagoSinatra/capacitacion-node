{
  "name": "lando1",
  "version": "0.0.1",
  "description": "CRUD basado en JSON hecho con Node",
  "main": "./src/app.js",
  "scripts": {
    "start": "node ./src/app.js",
    "startdev": "npx nodemon ./src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Santiago Sinatra",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
